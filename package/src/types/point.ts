/**
 * A position on the map view, in density-independent pixels from its top-left
 * corner - the unit and origin of the map view's own React Native layout.
 */
export interface Point {
  x: number;
  y: number;
}
